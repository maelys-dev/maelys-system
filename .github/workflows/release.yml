name: release

# Managed by maelys-release v0.41.0 (0.41.0). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@29d8be42e552233a44115aee930af3fb6d4e966b # v0.41.0
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: maelys-system
      tag: ${{ inputs.tag || github.ref_name }}
      linux_packages: build-essential dpkg-dev file rpm python3
      macos_packages: python3

  tap-libmaelys-sys:
    needs: release
    if: needs.release.result == 'success'
    uses: maelys-dev/maelys-release/.github/workflows/tap.yml@29d8be42e552233a44115aee930af3fb6d4e966b # v0.41.0
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: libmaelys-sys
      tag: ${{ inputs.tag || github.ref_name }}
      bottles: '["macos-15","macos-26"]'
    secrets:
      tap_token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
      tap_signing_key: ${{ secrets.HOMEBREW_TAP_SIGNING_KEY }}
