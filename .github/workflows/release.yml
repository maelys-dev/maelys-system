name: release

# Managed by maelys-release untagged (0.39.0). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@460dd8e26b598a36c6790a899e45bf0eb0652360 # untagged
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: maelys-system
      tag: ${{ inputs.tag || github.ref_name }}
      linux_packages: build-essential dpkg-dev file rpm python3
      macos_packages: python3

  tap-libmaelys-sys:
    needs: release
    if: needs.release.result == 'success'
    uses: maelys-dev/maelys-release/.github/workflows/tap.yml@460dd8e26b598a36c6790a899e45bf0eb0652360 # untagged
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: libmaelys-sys
      tag: ${{ inputs.tag || github.ref_name }}
      bottles: '["macos-15","macos-26"]'
    secrets:
      tap_token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
      tap_signing_key: ${{ secrets.HOMEBREW_TAP_SIGNING_KEY }}
